# Day 4 – 100_DAYS_DATA_GRIND

## Python Block

**Date:** Day 4  
**Focus:** Iterators, lambda, map, filter, list comprehensions, and modules.

### Topics Covered
- Iterators: `enumerate`, `zip`, `reversed`, `map`, `filter`
- Anonymous functions with `lambda`
- `map` and `filter` usage patterns
- List comprehensions for clean transformations
- Creating and importing a simple custom module (`math_utils`)
- Small utility functions (addition, multiply, abs_subtraction, division)

### Tasks & Exercises Completed
- Converted loops into list comprehensions (squared numbers, filter evens, convert to strings)
- Wrote lambdas for square, last-char extraction, and even-check
- Used `map` to curve grades and `filter` to extract passing grades
- Built `math_utils.py` with basic arithmetic functions and imported it in a main script
- Practiced iterator utilities (`zip`, `enumerate`, `reversed`, `map`, `filter`)

### Code Files (in this folder)
- `comprehensions_and_iterators.py` — list comprehensions, map/filter examples
- `lambda_practice.py` — lambda functions and usage patterns
- `math_utils.py` — custom module with functions
- `main_using_math_utils.py` — example showing importing and using the module

### Key Learnings
- List comprehensions + lambda/map/filter let you write short, readable data transformations.
- Custom modules keep code organized and reusable.
- Iterators like `enumerate` and `zip` are essential for clean loop code.

### Next Steps
- Apply these transformations inside real data flows (CSV → pandas).
- Finish SQL Block 2 (joins, anti-joins, subqueries) and start integrating SQL results into Python.
